/******/ (function(modules) { // webpackBootstrap
/******/ 	// The module cache
/******/ 	var installedModules = {};
/******/
/******/ 	// The require function
/******/ 	function __webpack_require__(moduleId) {
/******/
/******/ 		// Check if module is in cache
/******/ 		if(installedModules[moduleId]) {
/******/ 			return installedModules[moduleId].exports;
/******/ 		}
/******/ 		// Create a new module (and put it into the cache)
/******/ 		var module = installedModules[moduleId] = {
/******/ 			i: moduleId,
/******/ 			l: false,
/******/ 			exports: {}
/******/ 		};
/******/
/******/ 		// Execute the module function
/******/ 		modules[moduleId].call(module.exports, module, module.exports, __webpack_require__);
/******/
/******/ 		// Flag the module as loaded
/******/ 		module.l = true;
/******/
/******/ 		// Return the exports of the module
/******/ 		return module.exports;
/******/ 	}
/******/
/******/
/******/ 	// expose the modules object (__webpack_modules__)
/******/ 	__webpack_require__.m = modules;
/******/
/******/ 	// expose the module cache
/******/ 	__webpack_require__.c = installedModules;
/******/
/******/ 	// define getter function for harmony exports
/******/ 	__webpack_require__.d = function(exports, name, getter) {
/******/ 		if(!__webpack_require__.o(exports, name)) {
/******/ 			Object.defineProperty(exports, name, { enumerable: true, get: getter });
/******/ 		}
/******/ 	};
/******/
/******/ 	// define __esModule on exports
/******/ 	__webpack_require__.r = function(exports) {
/******/ 		if(typeof Symbol !== 'undefined' && Symbol.toStringTag) {
/******/ 			Object.defineProperty(exports, Symbol.toStringTag, { value: 'Module' });
/******/ 		}
/******/ 		Object.defineProperty(exports, '__esModule', { value: true });
/******/ 	};
/******/
/******/ 	// create a fake namespace object
/******/ 	// mode & 1: value is a module id, require it
/******/ 	// mode & 2: merge all properties of value into the ns
/******/ 	// mode & 4: return value when already ns object
/******/ 	// mode & 8|1: behave like require
/******/ 	__webpack_require__.t = function(value, mode) {
/******/ 		if(mode & 1) value = __webpack_require__(value);
/******/ 		if(mode & 8) return value;
/******/ 		if((mode & 4) && typeof value === 'object' && value && value.__esModule) return value;
/******/ 		var ns = Object.create(null);
/******/ 		__webpack_require__.r(ns);
/******/ 		Object.defineProperty(ns, 'default', { enumerable: true, value: value });
/******/ 		if(mode & 2 && typeof value != 'string') for(var key in value) __webpack_require__.d(ns, key, function(key) { return value[key]; }.bind(null, key));
/******/ 		return ns;
/******/ 	};
/******/
/******/ 	// getDefaultExport function for compatibility with non-harmony modules
/******/ 	__webpack_require__.n = function(module) {
/******/ 		var getter = module && module.__esModule ?
/******/ 			function getDefault() { return module['default']; } :
/******/ 			function getModuleExports() { return module; };
/******/ 		__webpack_require__.d(getter, 'a', getter);
/******/ 		return getter;
/******/ 	};
/******/
/******/ 	// Object.prototype.hasOwnProperty.call
/******/ 	__webpack_require__.o = function(object, property) { return Object.prototype.hasOwnProperty.call(object, property); };
/******/
/******/ 	// __webpack_public_path__
/******/ 	__webpack_require__.p = "";
/******/
/******/
/******/ 	// Load entry module and return exports
/******/ 	return __webpack_require__(__webpack_require__.s = 0);
/******/ })
/************************************************************************/
/******/ ({

/***/ "./node_modules/es6-promise/auto.js":
/*!******************************************!*\
  !*** ./node_modules/es6-promise/auto.js ***!
  \******************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

"use strict";
eval("// This file can be required in Browserify and Node.js for automatic polyfill\n// To use it:  require('es6-promise/auto');\n\nmodule.exports = __webpack_require__(/*! ./ */ \"./node_modules/es6-promise/dist/es6-promise.js\").polyfill();\n\n\n//# sourceURL=webpack:///./node_modules/es6-promise/auto.js?");

/***/ }),

/***/ "./node_modules/es6-promise/dist/es6-promise.js":
/*!******************************************************!*\
  !*** ./node_modules/es6-promise/dist/es6-promise.js ***!
  \******************************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("/* WEBPACK VAR INJECTION */(function(process, global) {/*!\n * @overview es6-promise - a tiny implementation of Promises/A+.\n * @copyright Copyright (c) 2014 Yehuda Katz, Tom Dale, Stefan Penner and contributors (Conversion to ES6 API by Jake Archibald)\n * @license   Licensed under MIT license\n *            See https://raw.githubusercontent.com/stefanpenner/es6-promise/master/LICENSE\n * @version   v4.2.4+314e4831\n */\n\n(function (global, factory) {\n\t true ? module.exports = factory() :\n\tundefined;\n}(this, (function () { 'use strict';\n\nfunction objectOrFunction(x) {\n  var type = typeof x;\n  return x !== null && (type === 'object' || type === 'function');\n}\n\nfunction isFunction(x) {\n  return typeof x === 'function';\n}\n\n\n\nvar _isArray = void 0;\nif (Array.isArray) {\n  _isArray = Array.isArray;\n} else {\n  _isArray = function (x) {\n    return Object.prototype.toString.call(x) === '[object Array]';\n  };\n}\n\nvar isArray = _isArray;\n\nvar len = 0;\nvar vertxNext = void 0;\nvar customSchedulerFn = void 0;\n\nvar asap = function asap(callback, arg) {\n  queue[len] = callback;\n  queue[len + 1] = arg;\n  len += 2;\n  if (len === 2) {\n    // If len is 2, that means that we need to schedule an async flush.\n    // If additional callbacks are queued before the queue is flushed, they\n    // will be processed by this flush that we are scheduling.\n    if (customSchedulerFn) {\n      customSchedulerFn(flush);\n    } else {\n      scheduleFlush();\n    }\n  }\n};\n\nfunction setScheduler(scheduleFn) {\n  customSchedulerFn = scheduleFn;\n}\n\nfunction setAsap(asapFn) {\n  asap = asapFn;\n}\n\nvar browserWindow = typeof window !== 'undefined' ? window : undefined;\nvar browserGlobal = browserWindow || {};\nvar BrowserMutationObserver = browserGlobal.MutationObserver || browserGlobal.WebKitMutationObserver;\nvar isNode = typeof self === 'undefined' && typeof process !== 'undefined' && {}.toString.call(process) === '[object process]';\n\n// test for web worker but not in IE10\nvar isWorker = typeof Uint8ClampedArray !== 'undefined' && typeof importScripts !== 'undefined' && typeof MessageChannel !== 'undefined';\n\n// node\nfunction useNextTick() {\n  // node version 0.10.x displays a deprecation warning when nextTick is used recursively\n  // see https://github.com/cujojs/when/issues/410 for details\n  return function () {\n    return process.nextTick(flush);\n  };\n}\n\n// vertx\nfunction useVertxTimer() {\n  if (typeof vertxNext !== 'undefined') {\n    return function () {\n      vertxNext(flush);\n    };\n  }\n\n  return useSetTimeout();\n}\n\nfunction useMutationObserver() {\n  var iterations = 0;\n  var observer = new BrowserMutationObserver(flush);\n  var node = document.createTextNode('');\n  observer.observe(node, { characterData: true });\n\n  return function () {\n    node.data = iterations = ++iterations % 2;\n  };\n}\n\n// web worker\nfunction useMessageChannel() {\n  var channel = new MessageChannel();\n  channel.port1.onmessage = flush;\n  return function () {\n    return channel.port2.postMessage(0);\n  };\n}\n\nfunction useSetTimeout() {\n  // Store setTimeout reference so es6-promise will be unaffected by\n  // other code modifying setTimeout (like sinon.useFakeTimers())\n  var globalSetTimeout = setTimeout;\n  return function () {\n    return globalSetTimeout(flush, 1);\n  };\n}\n\nvar queue = new Array(1000);\nfunction flush() {\n  for (var i = 0; i < len; i += 2) {\n    var callback = queue[i];\n    var arg = queue[i + 1];\n\n    callback(arg);\n\n    queue[i] = undefined;\n    queue[i + 1] = undefined;\n  }\n\n  len = 0;\n}\n\nfunction attemptVertx() {\n  try {\n    var vertx = Function('return this')().require('vertx');\n    vertxNext = vertx.runOnLoop || vertx.runOnContext;\n    return useVertxTimer();\n  } catch (e) {\n    return useSetTimeout();\n  }\n}\n\nvar scheduleFlush = void 0;\n// Decide what async method to use to triggering processing of queued callbacks:\nif (isNode) {\n  scheduleFlush = useNextTick();\n} else if (BrowserMutationObserver) {\n  scheduleFlush = useMutationObserver();\n} else if (isWorker) {\n  scheduleFlush = useMessageChannel();\n} else if (browserWindow === undefined && \"function\" === 'function') {\n  scheduleFlush = attemptVertx();\n} else {\n  scheduleFlush = useSetTimeout();\n}\n\nfunction then(onFulfillment, onRejection) {\n  var parent = this;\n\n  var child = new this.constructor(noop);\n\n  if (child[PROMISE_ID] === undefined) {\n    makePromise(child);\n  }\n\n  var _state = parent._state;\n\n\n  if (_state) {\n    var callback = arguments[_state - 1];\n    asap(function () {\n      return invokeCallback(_state, child, callback, parent._result);\n    });\n  } else {\n    subscribe(parent, child, onFulfillment, onRejection);\n  }\n\n  return child;\n}\n\n/**\n  `Promise.resolve` returns a promise that will become resolved with the\n  passed `value`. It is shorthand for the following:\n\n  ```javascript\n  let promise = new Promise(function(resolve, reject){\n    resolve(1);\n  });\n\n  promise.then(function(value){\n    // value === 1\n  });\n  ```\n\n  Instead of writing the above, your code now simply becomes the following:\n\n  ```javascript\n  let promise = Promise.resolve(1);\n\n  promise.then(function(value){\n    // value === 1\n  });\n  ```\n\n  @method resolve\n  @static\n  @param {Any} value value that the returned promise will be resolved with\n  Useful for tooling.\n  @return {Promise} a promise that will become fulfilled with the given\n  `value`\n*/\nfunction resolve$1(object) {\n  /*jshint validthis:true */\n  var Constructor = this;\n\n  if (object && typeof object === 'object' && object.constructor === Constructor) {\n    return object;\n  }\n\n  var promise = new Constructor(noop);\n  resolve(promise, object);\n  return promise;\n}\n\nvar PROMISE_ID = Math.random().toString(36).substring(2);\n\nfunction noop() {}\n\nvar PENDING = void 0;\nvar FULFILLED = 1;\nvar REJECTED = 2;\n\nvar TRY_CATCH_ERROR = { error: null };\n\nfunction selfFulfillment() {\n  return new TypeError(\"You cannot resolve a promise with itself\");\n}\n\nfunction cannotReturnOwn() {\n  return new TypeError('A promises callback cannot return that same promise.');\n}\n\nfunction getThen(promise) {\n  try {\n    return promise.then;\n  } catch (error) {\n    TRY_CATCH_ERROR.error = error;\n    return TRY_CATCH_ERROR;\n  }\n}\n\nfunction tryThen(then$$1, value, fulfillmentHandler, rejectionHandler) {\n  try {\n    then$$1.call(value, fulfillmentHandler, rejectionHandler);\n  } catch (e) {\n    return e;\n  }\n}\n\nfunction handleForeignThenable(promise, thenable, then$$1) {\n  asap(function (promise) {\n    var sealed = false;\n    var error = tryThen(then$$1, thenable, function (value) {\n      if (sealed) {\n        return;\n      }\n      sealed = true;\n      if (thenable !== value) {\n        resolve(promise, value);\n      } else {\n        fulfill(promise, value);\n      }\n    }, function (reason) {\n      if (sealed) {\n        return;\n      }\n      sealed = true;\n\n      reject(promise, reason);\n    }, 'Settle: ' + (promise._label || ' unknown promise'));\n\n    if (!sealed && error) {\n      sealed = true;\n      reject(promise, error);\n    }\n  }, promise);\n}\n\nfunction handleOwnThenable(promise, thenable) {\n  if (thenable._state === FULFILLED) {\n    fulfill(promise, thenable._result);\n  } else if (thenable._state === REJECTED) {\n    reject(promise, thenable._result);\n  } else {\n    subscribe(thenable, undefined, function (value) {\n      return resolve(promise, value);\n    }, function (reason) {\n      return reject(promise, reason);\n    });\n  }\n}\n\nfunction handleMaybeThenable(promise, maybeThenable, then$$1) {\n  if (maybeThenable.constructor === promise.constructor && then$$1 === then && maybeThenable.constructor.resolve === resolve$1) {\n    handleOwnThenable(promise, maybeThenable);\n  } else {\n    if (then$$1 === TRY_CATCH_ERROR) {\n      reject(promise, TRY_CATCH_ERROR.error);\n      TRY_CATCH_ERROR.error = null;\n    } else if (then$$1 === undefined) {\n      fulfill(promise, maybeThenable);\n    } else if (isFunction(then$$1)) {\n      handleForeignThenable(promise, maybeThenable, then$$1);\n    } else {\n      fulfill(promise, maybeThenable);\n    }\n  }\n}\n\nfunction resolve(promise, value) {\n  if (promise === value) {\n    reject(promise, selfFulfillment());\n  } else if (objectOrFunction(value)) {\n    handleMaybeThenable(promise, value, getThen(value));\n  } else {\n    fulfill(promise, value);\n  }\n}\n\nfunction publishRejection(promise) {\n  if (promise._onerror) {\n    promise._onerror(promise._result);\n  }\n\n  publish(promise);\n}\n\nfunction fulfill(promise, value) {\n  if (promise._state !== PENDING) {\n    return;\n  }\n\n  promise._result = value;\n  promise._state = FULFILLED;\n\n  if (promise._subscribers.length !== 0) {\n    asap(publish, promise);\n  }\n}\n\nfunction reject(promise, reason) {\n  if (promise._state !== PENDING) {\n    return;\n  }\n  promise._state = REJECTED;\n  promise._result = reason;\n\n  asap(publishRejection, promise);\n}\n\nfunction subscribe(parent, child, onFulfillment, onRejection) {\n  var _subscribers = parent._subscribers;\n  var length = _subscribers.length;\n\n\n  parent._onerror = null;\n\n  _subscribers[length] = child;\n  _subscribers[length + FULFILLED] = onFulfillment;\n  _subscribers[length + REJECTED] = onRejection;\n\n  if (length === 0 && parent._state) {\n    asap(publish, parent);\n  }\n}\n\nfunction publish(promise) {\n  var subscribers = promise._subscribers;\n  var settled = promise._state;\n\n  if (subscribers.length === 0) {\n    return;\n  }\n\n  var child = void 0,\n      callback = void 0,\n      detail = promise._result;\n\n  for (var i = 0; i < subscribers.length; i += 3) {\n    child = subscribers[i];\n    callback = subscribers[i + settled];\n\n    if (child) {\n      invokeCallback(settled, child, callback, detail);\n    } else {\n      callback(detail);\n    }\n  }\n\n  promise._subscribers.length = 0;\n}\n\nfunction tryCatch(callback, detail) {\n  try {\n    return callback(detail);\n  } catch (e) {\n    TRY_CATCH_ERROR.error = e;\n    return TRY_CATCH_ERROR;\n  }\n}\n\nfunction invokeCallback(settled, promise, callback, detail) {\n  var hasCallback = isFunction(callback),\n      value = void 0,\n      error = void 0,\n      succeeded = void 0,\n      failed = void 0;\n\n  if (hasCallback) {\n    value = tryCatch(callback, detail);\n\n    if (value === TRY_CATCH_ERROR) {\n      failed = true;\n      error = value.error;\n      value.error = null;\n    } else {\n      succeeded = true;\n    }\n\n    if (promise === value) {\n      reject(promise, cannotReturnOwn());\n      return;\n    }\n  } else {\n    value = detail;\n    succeeded = true;\n  }\n\n  if (promise._state !== PENDING) {\n    // noop\n  } else if (hasCallback && succeeded) {\n    resolve(promise, value);\n  } else if (failed) {\n    reject(promise, error);\n  } else if (settled === FULFILLED) {\n    fulfill(promise, value);\n  } else if (settled === REJECTED) {\n    reject(promise, value);\n  }\n}\n\nfunction initializePromise(promise, resolver) {\n  try {\n    resolver(function resolvePromise(value) {\n      resolve(promise, value);\n    }, function rejectPromise(reason) {\n      reject(promise, reason);\n    });\n  } catch (e) {\n    reject(promise, e);\n  }\n}\n\nvar id = 0;\nfunction nextId() {\n  return id++;\n}\n\nfunction makePromise(promise) {\n  promise[PROMISE_ID] = id++;\n  promise._state = undefined;\n  promise._result = undefined;\n  promise._subscribers = [];\n}\n\nfunction validationError() {\n  return new Error('Array Methods must be provided an Array');\n}\n\nvar Enumerator = function () {\n  function Enumerator(Constructor, input) {\n    this._instanceConstructor = Constructor;\n    this.promise = new Constructor(noop);\n\n    if (!this.promise[PROMISE_ID]) {\n      makePromise(this.promise);\n    }\n\n    if (isArray(input)) {\n      this.length = input.length;\n      this._remaining = input.length;\n\n      this._result = new Array(this.length);\n\n      if (this.length === 0) {\n        fulfill(this.promise, this._result);\n      } else {\n        this.length = this.length || 0;\n        this._enumerate(input);\n        if (this._remaining === 0) {\n          fulfill(this.promise, this._result);\n        }\n      }\n    } else {\n      reject(this.promise, validationError());\n    }\n  }\n\n  Enumerator.prototype._enumerate = function _enumerate(input) {\n    for (var i = 0; this._state === PENDING && i < input.length; i++) {\n      this._eachEntry(input[i], i);\n    }\n  };\n\n  Enumerator.prototype._eachEntry = function _eachEntry(entry, i) {\n    var c = this._instanceConstructor;\n    var resolve$$1 = c.resolve;\n\n\n    if (resolve$$1 === resolve$1) {\n      var _then = getThen(entry);\n\n      if (_then === then && entry._state !== PENDING) {\n        this._settledAt(entry._state, i, entry._result);\n      } else if (typeof _then !== 'function') {\n        this._remaining--;\n        this._result[i] = entry;\n      } else if (c === Promise$1) {\n        var promise = new c(noop);\n        handleMaybeThenable(promise, entry, _then);\n        this._willSettleAt(promise, i);\n      } else {\n        this._willSettleAt(new c(function (resolve$$1) {\n          return resolve$$1(entry);\n        }), i);\n      }\n    } else {\n      this._willSettleAt(resolve$$1(entry), i);\n    }\n  };\n\n  Enumerator.prototype._settledAt = function _settledAt(state, i, value) {\n    var promise = this.promise;\n\n\n    if (promise._state === PENDING) {\n      this._remaining--;\n\n      if (state === REJECTED) {\n        reject(promise, value);\n      } else {\n        this._result[i] = value;\n      }\n    }\n\n    if (this._remaining === 0) {\n      fulfill(promise, this._result);\n    }\n  };\n\n  Enumerator.prototype._willSettleAt = function _willSettleAt(promise, i) {\n    var enumerator = this;\n\n    subscribe(promise, undefined, function (value) {\n      return enumerator._settledAt(FULFILLED, i, value);\n    }, function (reason) {\n      return enumerator._settledAt(REJECTED, i, reason);\n    });\n  };\n\n  return Enumerator;\n}();\n\n/**\n  `Promise.all` accepts an array of promises, and returns a new promise which\n  is fulfilled with an array of fulfillment values for the passed promises, or\n  rejected with the reason of the first passed promise to be rejected. It casts all\n  elements of the passed iterable to promises as it runs this algorithm.\n\n  Example:\n\n  ```javascript\n  let promise1 = resolve(1);\n  let promise2 = resolve(2);\n  let promise3 = resolve(3);\n  let promises = [ promise1, promise2, promise3 ];\n\n  Promise.all(promises).then(function(array){\n    // The array here would be [ 1, 2, 3 ];\n  });\n  ```\n\n  If any of the `promises` given to `all` are rejected, the first promise\n  that is rejected will be given as an argument to the returned promises's\n  rejection handler. For example:\n\n  Example:\n\n  ```javascript\n  let promise1 = resolve(1);\n  let promise2 = reject(new Error(\"2\"));\n  let promise3 = reject(new Error(\"3\"));\n  let promises = [ promise1, promise2, promise3 ];\n\n  Promise.all(promises).then(function(array){\n    // Code here never runs because there are rejected promises!\n  }, function(error) {\n    // error.message === \"2\"\n  });\n  ```\n\n  @method all\n  @static\n  @param {Array} entries array of promises\n  @param {String} label optional string for labeling the promise.\n  Useful for tooling.\n  @return {Promise} promise that is fulfilled when all `promises` have been\n  fulfilled, or rejected if any of them become rejected.\n  @static\n*/\nfunction all(entries) {\n  return new Enumerator(this, entries).promise;\n}\n\n/**\n  `Promise.race` returns a new promise which is settled in the same way as the\n  first passed promise to settle.\n\n  Example:\n\n  ```javascript\n  let promise1 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 1');\n    }, 200);\n  });\n\n  let promise2 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 2');\n    }, 100);\n  });\n\n  Promise.race([promise1, promise2]).then(function(result){\n    // result === 'promise 2' because it was resolved before promise1\n    // was resolved.\n  });\n  ```\n\n  `Promise.race` is deterministic in that only the state of the first\n  settled promise matters. For example, even if other promises given to the\n  `promises` array argument are resolved, but the first settled promise has\n  become rejected before the other promises became fulfilled, the returned\n  promise will become rejected:\n\n  ```javascript\n  let promise1 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      resolve('promise 1');\n    }, 200);\n  });\n\n  let promise2 = new Promise(function(resolve, reject){\n    setTimeout(function(){\n      reject(new Error('promise 2'));\n    }, 100);\n  });\n\n  Promise.race([promise1, promise2]).then(function(result){\n    // Code here never runs\n  }, function(reason){\n    // reason.message === 'promise 2' because promise 2 became rejected before\n    // promise 1 became fulfilled\n  });\n  ```\n\n  An example real-world use case is implementing timeouts:\n\n  ```javascript\n  Promise.race([ajax('foo.json'), timeout(5000)])\n  ```\n\n  @method race\n  @static\n  @param {Array} promises array of promises to observe\n  Useful for tooling.\n  @return {Promise} a promise which settles in the same way as the first passed\n  promise to settle.\n*/\nfunction race(entries) {\n  /*jshint validthis:true */\n  var Constructor = this;\n\n  if (!isArray(entries)) {\n    return new Constructor(function (_, reject) {\n      return reject(new TypeError('You must pass an array to race.'));\n    });\n  } else {\n    return new Constructor(function (resolve, reject) {\n      var length = entries.length;\n      for (var i = 0; i < length; i++) {\n        Constructor.resolve(entries[i]).then(resolve, reject);\n      }\n    });\n  }\n}\n\n/**\n  `Promise.reject` returns a promise rejected with the passed `reason`.\n  It is shorthand for the following:\n\n  ```javascript\n  let promise = new Promise(function(resolve, reject){\n    reject(new Error('WHOOPS'));\n  });\n\n  promise.then(function(value){\n    // Code here doesn't run because the promise is rejected!\n  }, function(reason){\n    // reason.message === 'WHOOPS'\n  });\n  ```\n\n  Instead of writing the above, your code now simply becomes the following:\n\n  ```javascript\n  let promise = Promise.reject(new Error('WHOOPS'));\n\n  promise.then(function(value){\n    // Code here doesn't run because the promise is rejected!\n  }, function(reason){\n    // reason.message === 'WHOOPS'\n  });\n  ```\n\n  @method reject\n  @static\n  @param {Any} reason value that the returned promise will be rejected with.\n  Useful for tooling.\n  @return {Promise} a promise rejected with the given `reason`.\n*/\nfunction reject$1(reason) {\n  /*jshint validthis:true */\n  var Constructor = this;\n  var promise = new Constructor(noop);\n  reject(promise, reason);\n  return promise;\n}\n\nfunction needsResolver() {\n  throw new TypeError('You must pass a resolver function as the first argument to the promise constructor');\n}\n\nfunction needsNew() {\n  throw new TypeError(\"Failed to construct 'Promise': Please use the 'new' operator, this object constructor cannot be called as a function.\");\n}\n\n/**\n  Promise objects represent the eventual result of an asynchronous operation. The\n  primary way of interacting with a promise is through its `then` method, which\n  registers callbacks to receive either a promise's eventual value or the reason\n  why the promise cannot be fulfilled.\n\n  Terminology\n  -----------\n\n  - `promise` is an object or function with a `then` method whose behavior conforms to this specification.\n  - `thenable` is an object or function that defines a `then` method.\n  - `value` is any legal JavaScript value (including undefined, a thenable, or a promise).\n  - `exception` is a value that is thrown using the throw statement.\n  - `reason` is a value that indicates why a promise was rejected.\n  - `settled` the final resting state of a promise, fulfilled or rejected.\n\n  A promise can be in one of three states: pending, fulfilled, or rejected.\n\n  Promises that are fulfilled have a fulfillment value and are in the fulfilled\n  state.  Promises that are rejected have a rejection reason and are in the\n  rejected state.  A fulfillment value is never a thenable.\n\n  Promises can also be said to *resolve* a value.  If this value is also a\n  promise, then the original promise's settled state will match the value's\n  settled state.  So a promise that *resolves* a promise that rejects will\n  itself reject, and a promise that *resolves* a promise that fulfills will\n  itself fulfill.\n\n\n  Basic Usage:\n  ------------\n\n  ```js\n  let promise = new Promise(function(resolve, reject) {\n    // on success\n    resolve(value);\n\n    // on failure\n    reject(reason);\n  });\n\n  promise.then(function(value) {\n    // on fulfillment\n  }, function(reason) {\n    // on rejection\n  });\n  ```\n\n  Advanced Usage:\n  ---------------\n\n  Promises shine when abstracting away asynchronous interactions such as\n  `XMLHttpRequest`s.\n\n  ```js\n  function getJSON(url) {\n    return new Promise(function(resolve, reject){\n      let xhr = new XMLHttpRequest();\n\n      xhr.open('GET', url);\n      xhr.onreadystatechange = handler;\n      xhr.responseType = 'json';\n      xhr.setRequestHeader('Accept', 'application/json');\n      xhr.send();\n\n      function handler() {\n        if (this.readyState === this.DONE) {\n          if (this.status === 200) {\n            resolve(this.response);\n          } else {\n            reject(new Error('getJSON: `' + url + '` failed with status: [' + this.status + ']'));\n          }\n        }\n      };\n    });\n  }\n\n  getJSON('/posts.json').then(function(json) {\n    // on fulfillment\n  }, function(reason) {\n    // on rejection\n  });\n  ```\n\n  Unlike callbacks, promises are great composable primitives.\n\n  ```js\n  Promise.all([\n    getJSON('/posts'),\n    getJSON('/comments')\n  ]).then(function(values){\n    values[0] // => postsJSON\n    values[1] // => commentsJSON\n\n    return values;\n  });\n  ```\n\n  @class Promise\n  @param {Function} resolver\n  Useful for tooling.\n  @constructor\n*/\n\nvar Promise$1 = function () {\n  function Promise(resolver) {\n    this[PROMISE_ID] = nextId();\n    this._result = this._state = undefined;\n    this._subscribers = [];\n\n    if (noop !== resolver) {\n      typeof resolver !== 'function' && needsResolver();\n      this instanceof Promise ? initializePromise(this, resolver) : needsNew();\n    }\n  }\n\n  /**\n  The primary way of interacting with a promise is through its `then` method,\n  which registers callbacks to receive either a promise's eventual value or the\n  reason why the promise cannot be fulfilled.\n   ```js\n  findUser().then(function(user){\n    // user is available\n  }, function(reason){\n    // user is unavailable, and you are given the reason why\n  });\n  ```\n   Chaining\n  --------\n   The return value of `then` is itself a promise.  This second, 'downstream'\n  promise is resolved with the return value of the first promise's fulfillment\n  or rejection handler, or rejected if the handler throws an exception.\n   ```js\n  findUser().then(function (user) {\n    return user.name;\n  }, function (reason) {\n    return 'default name';\n  }).then(function (userName) {\n    // If `findUser` fulfilled, `userName` will be the user's name, otherwise it\n    // will be `'default name'`\n  });\n   findUser().then(function (user) {\n    throw new Error('Found user, but still unhappy');\n  }, function (reason) {\n    throw new Error('`findUser` rejected and we're unhappy');\n  }).then(function (value) {\n    // never reached\n  }, function (reason) {\n    // if `findUser` fulfilled, `reason` will be 'Found user, but still unhappy'.\n    // If `findUser` rejected, `reason` will be '`findUser` rejected and we're unhappy'.\n  });\n  ```\n  If the downstream promise does not specify a rejection handler, rejection reasons will be propagated further downstream.\n   ```js\n  findUser().then(function (user) {\n    throw new PedagogicalException('Upstream error');\n  }).then(function (value) {\n    // never reached\n  }).then(function (value) {\n    // never reached\n  }, function (reason) {\n    // The `PedgagocialException` is propagated all the way down to here\n  });\n  ```\n   Assimilation\n  ------------\n   Sometimes the value you want to propagate to a downstream promise can only be\n  retrieved asynchronously. This can be achieved by returning a promise in the\n  fulfillment or rejection handler. The downstream promise will then be pending\n  until the returned promise is settled. This is called *assimilation*.\n   ```js\n  findUser().then(function (user) {\n    return findCommentsByAuthor(user);\n  }).then(function (comments) {\n    // The user's comments are now available\n  });\n  ```\n   If the assimliated promise rejects, then the downstream promise will also reject.\n   ```js\n  findUser().then(function (user) {\n    return findCommentsByAuthor(user);\n  }).then(function (comments) {\n    // If `findCommentsByAuthor` fulfills, we'll have the value here\n  }, function (reason) {\n    // If `findCommentsByAuthor` rejects, we'll have the reason here\n  });\n  ```\n   Simple Example\n  --------------\n   Synchronous Example\n   ```javascript\n  let result;\n   try {\n    result = findResult();\n    // success\n  } catch(reason) {\n    // failure\n  }\n  ```\n   Errback Example\n   ```js\n  findResult(function(result, err){\n    if (err) {\n      // failure\n    } else {\n      // success\n    }\n  });\n  ```\n   Promise Example;\n   ```javascript\n  findResult().then(function(result){\n    // success\n  }, function(reason){\n    // failure\n  });\n  ```\n   Advanced Example\n  --------------\n   Synchronous Example\n   ```javascript\n  let author, books;\n   try {\n    author = findAuthor();\n    books  = findBooksByAuthor(author);\n    // success\n  } catch(reason) {\n    // failure\n  }\n  ```\n   Errback Example\n   ```js\n   function foundBooks(books) {\n   }\n   function failure(reason) {\n   }\n   findAuthor(function(author, err){\n    if (err) {\n      failure(err);\n      // failure\n    } else {\n      try {\n        findBoooksByAuthor(author, function(books, err) {\n          if (err) {\n            failure(err);\n          } else {\n            try {\n              foundBooks(books);\n            } catch(reason) {\n              failure(reason);\n            }\n          }\n        });\n      } catch(error) {\n        failure(err);\n      }\n      // success\n    }\n  });\n  ```\n   Promise Example;\n   ```javascript\n  findAuthor().\n    then(findBooksByAuthor).\n    then(function(books){\n      // found books\n  }).catch(function(reason){\n    // something went wrong\n  });\n  ```\n   @method then\n  @param {Function} onFulfilled\n  @param {Function} onRejected\n  Useful for tooling.\n  @return {Promise}\n  */\n\n  /**\n  `catch` is simply sugar for `then(undefined, onRejection)` which makes it the same\n  as the catch block of a try/catch statement.\n  ```js\n  function findAuthor(){\n  throw new Error('couldn't find that author');\n  }\n  // synchronous\n  try {\n  findAuthor();\n  } catch(reason) {\n  // something went wrong\n  }\n  // async with promises\n  findAuthor().catch(function(reason){\n  // something went wrong\n  });\n  ```\n  @method catch\n  @param {Function} onRejection\n  Useful for tooling.\n  @return {Promise}\n  */\n\n\n  Promise.prototype.catch = function _catch(onRejection) {\n    return this.then(null, onRejection);\n  };\n\n  /**\n    `finally` will be invoked regardless of the promise's fate just as native\n    try/catch/finally behaves\n  \n    Synchronous example:\n  \n    ```js\n    findAuthor() {\n      if (Math.random() > 0.5) {\n        throw new Error();\n      }\n      return new Author();\n    }\n  \n    try {\n      return findAuthor(); // succeed or fail\n    } catch(error) {\n      return findOtherAuther();\n    } finally {\n      // always runs\n      // doesn't affect the return value\n    }\n    ```\n  \n    Asynchronous example:\n  \n    ```js\n    findAuthor().catch(function(reason){\n      return findOtherAuther();\n    }).finally(function(){\n      // author was either found, or not\n    });\n    ```\n  \n    @method finally\n    @param {Function} callback\n    @return {Promise}\n  */\n\n\n  Promise.prototype.finally = function _finally(callback) {\n    var promise = this;\n    var constructor = promise.constructor;\n\n    return promise.then(function (value) {\n      return constructor.resolve(callback()).then(function () {\n        return value;\n      });\n    }, function (reason) {\n      return constructor.resolve(callback()).then(function () {\n        throw reason;\n      });\n    });\n  };\n\n  return Promise;\n}();\n\nPromise$1.prototype.then = then;\nPromise$1.all = all;\nPromise$1.race = race;\nPromise$1.resolve = resolve$1;\nPromise$1.reject = reject$1;\nPromise$1._setScheduler = setScheduler;\nPromise$1._setAsap = setAsap;\nPromise$1._asap = asap;\n\n/*global self*/\nfunction polyfill() {\n  var local = void 0;\n\n  if (typeof global !== 'undefined') {\n    local = global;\n  } else if (typeof self !== 'undefined') {\n    local = self;\n  } else {\n    try {\n      local = Function('return this')();\n    } catch (e) {\n      throw new Error('polyfill failed because global object is unavailable in this environment');\n    }\n  }\n\n  var P = local.Promise;\n\n  if (P) {\n    var promiseToString = null;\n    try {\n      promiseToString = Object.prototype.toString.call(P.resolve());\n    } catch (e) {\n      // silently ignored\n    }\n\n    if (promiseToString === '[object Promise]' && !P.cast) {\n      return;\n    }\n  }\n\n  local.Promise = Promise$1;\n}\n\n// Strange compat..\nPromise$1.polyfill = polyfill;\nPromise$1.Promise = Promise$1;\n\nreturn Promise$1;\n\n})));\n\n\n\n//# sourceMappingURL=es6-promise.map\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../process/browser.js */ \"./node_modules/process/browser.js\"), __webpack_require__(/*! ./../../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/es6-promise/dist/es6-promise.js?");

/***/ }),

/***/ "./node_modules/process/browser.js":
/*!*****************************************!*\
  !*** ./node_modules/process/browser.js ***!
  \*****************************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("// shim for using process in browser\nvar process = module.exports = {};\n\n// cached from whatever global is present so that test runners that stub it\n// don't break things.  But we need to wrap it in a try catch in case it is\n// wrapped in strict mode code which doesn't define any globals.  It's inside a\n// function because try/catches deoptimize in certain engines.\n\nvar cachedSetTimeout;\nvar cachedClearTimeout;\n\nfunction defaultSetTimout() {\n    throw new Error('setTimeout has not been defined');\n}\nfunction defaultClearTimeout () {\n    throw new Error('clearTimeout has not been defined');\n}\n(function () {\n    try {\n        if (typeof setTimeout === 'function') {\n            cachedSetTimeout = setTimeout;\n        } else {\n            cachedSetTimeout = defaultSetTimout;\n        }\n    } catch (e) {\n        cachedSetTimeout = defaultSetTimout;\n    }\n    try {\n        if (typeof clearTimeout === 'function') {\n            cachedClearTimeout = clearTimeout;\n        } else {\n            cachedClearTimeout = defaultClearTimeout;\n        }\n    } catch (e) {\n        cachedClearTimeout = defaultClearTimeout;\n    }\n} ())\nfunction runTimeout(fun) {\n    if (cachedSetTimeout === setTimeout) {\n        //normal enviroments in sane situations\n        return setTimeout(fun, 0);\n    }\n    // if setTimeout wasn't available but was latter defined\n    if ((cachedSetTimeout === defaultSetTimout || !cachedSetTimeout) && setTimeout) {\n        cachedSetTimeout = setTimeout;\n        return setTimeout(fun, 0);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedSetTimeout(fun, 0);\n    } catch(e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't trust the global object when called normally\n            return cachedSetTimeout.call(null, fun, 0);\n        } catch(e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error\n            return cachedSetTimeout.call(this, fun, 0);\n        }\n    }\n\n\n}\nfunction runClearTimeout(marker) {\n    if (cachedClearTimeout === clearTimeout) {\n        //normal enviroments in sane situations\n        return clearTimeout(marker);\n    }\n    // if clearTimeout wasn't available but was latter defined\n    if ((cachedClearTimeout === defaultClearTimeout || !cachedClearTimeout) && clearTimeout) {\n        cachedClearTimeout = clearTimeout;\n        return clearTimeout(marker);\n    }\n    try {\n        // when when somebody has screwed with setTimeout but no I.E. maddness\n        return cachedClearTimeout(marker);\n    } catch (e){\n        try {\n            // When we are in I.E. but the script has been evaled so I.E. doesn't  trust the global object when called normally\n            return cachedClearTimeout.call(null, marker);\n        } catch (e){\n            // same as above but when it's a version of I.E. that must have the global object for 'this', hopfully our context correct otherwise it will throw a global error.\n            // Some versions of I.E. have different rules for clearTimeout vs setTimeout\n            return cachedClearTimeout.call(this, marker);\n        }\n    }\n\n\n\n}\nvar queue = [];\nvar draining = false;\nvar currentQueue;\nvar queueIndex = -1;\n\nfunction cleanUpNextTick() {\n    if (!draining || !currentQueue) {\n        return;\n    }\n    draining = false;\n    if (currentQueue.length) {\n        queue = currentQueue.concat(queue);\n    } else {\n        queueIndex = -1;\n    }\n    if (queue.length) {\n        drainQueue();\n    }\n}\n\nfunction drainQueue() {\n    if (draining) {\n        return;\n    }\n    var timeout = runTimeout(cleanUpNextTick);\n    draining = true;\n\n    var len = queue.length;\n    while(len) {\n        currentQueue = queue;\n        queue = [];\n        while (++queueIndex < len) {\n            if (currentQueue) {\n                currentQueue[queueIndex].run();\n            }\n        }\n        queueIndex = -1;\n        len = queue.length;\n    }\n    currentQueue = null;\n    draining = false;\n    runClearTimeout(timeout);\n}\n\nprocess.nextTick = function (fun) {\n    var args = new Array(arguments.length - 1);\n    if (arguments.length > 1) {\n        for (var i = 1; i < arguments.length; i++) {\n            args[i - 1] = arguments[i];\n        }\n    }\n    queue.push(new Item(fun, args));\n    if (queue.length === 1 && !draining) {\n        runTimeout(drainQueue);\n    }\n};\n\n// v8 likes predictible objects\nfunction Item(fun, array) {\n    this.fun = fun;\n    this.array = array;\n}\nItem.prototype.run = function () {\n    this.fun.apply(null, this.array);\n};\nprocess.title = 'browser';\nprocess.browser = true;\nprocess.env = {};\nprocess.argv = [];\nprocess.version = ''; // empty string to avoid regexp issues\nprocess.versions = {};\n\nfunction noop() {}\n\nprocess.on = noop;\nprocess.addListener = noop;\nprocess.once = noop;\nprocess.off = noop;\nprocess.removeListener = noop;\nprocess.removeAllListeners = noop;\nprocess.emit = noop;\nprocess.prependListener = noop;\nprocess.prependOnceListener = noop;\n\nprocess.listeners = function (name) { return [] }\n\nprocess.binding = function (name) {\n    throw new Error('process.binding is not supported');\n};\n\nprocess.cwd = function () { return '/' };\nprocess.chdir = function (dir) {\n    throw new Error('process.chdir is not supported');\n};\nprocess.umask = function() { return 0; };\n\n\n//# sourceURL=webpack:///./node_modules/process/browser.js?");

/***/ }),

/***/ "./node_modules/webpack/buildin/global.js":
/*!***********************************!*\
  !*** (webpack)/buildin/global.js ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports) {

eval("var g;\r\n\r\n// This works in non-strict mode\r\ng = (function() {\r\n\treturn this;\r\n})();\r\n\r\ntry {\r\n\t// This works if eval is allowed (see CSP)\r\n\tg = g || Function(\"return this\")() || (1, eval)(\"this\");\r\n} catch (e) {\r\n\t// This works if the window reference is available\r\n\tif (typeof window === \"object\") g = window;\r\n}\r\n\r\n// g can still be undefined, but nothing to do about it...\r\n// We return undefined, instead of nothing here, so it's\r\n// easier to handle this case. if(!global) { ...}\r\n\r\nmodule.exports = g;\r\n\n\n//# sourceURL=webpack:///(webpack)/buildin/global.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/core/core.js":
/*!****************************************************!*\
  !*** ./node_modules/wee-core/scripts/core/core.js ***!
  \****************************************************/
/*! exports provided: isBrowser, $env, $envSecure, $envReset, $exec */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"isBrowser\", function() { return isBrowser; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$env\", function() { return $env; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$envSecure\", function() { return $envSecure; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$envReset\", function() { return $envReset; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$exec\", function() { return $exec; });\n/* harmony import */ var _types__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./types */ \"./node_modules/wee-core/scripts/core/types.js\");\n\n\nconst isBrowser = typeof window === 'object';\n\nlet env;\n\n/**\n * Get value from function or directly\n *\n * @private\n * @param {*} val\n * @param {Object} [options]\n * @returns {*}\n */\nfunction _val(val, options) {\n    return Object(_types__WEBPACK_IMPORTED_MODULE_0__[\"$isFunction\"])(val) ?\n        $exec(val, options) :\n        val;\n}\n\n/**\n * Get current environment or set current environment against\n * specified Object\n *\n * @param {Object} [rules]\n * @param {string} [fallback=local]\n * @returns {string} environment\n */\nfunction $env(rules, fallback = 'local') {\n    if (rules) {\n        const host = location.hostname;\n\n        for (const rule in rules) {\n            const val = rules[rule];\n\n            if (val == host || _val(val, {\n                args: host,\n            }) === true) {\n                env = rule;\n                break;\n            }\n        }\n\n        if (! env) {\n            env = fallback;\n        }\n    }\n\n    return env || fallback;\n}\n\n/**\n * Determine if the current environment is SSL encrypted\n *\n * @returns {boolean} secure\n */\nfunction $envSecure() {\n    return location.protocol == 'https:';\n}\n\n/**\n * Reset env variable - used for testing\n */\nfunction $envReset() {\n    env = undefined;\n}\n\n/**\n * Execute specified function or Array of functions\n *\n * @param {Array|function} fn\n * @param {Object} [options]\n * @param {Array} [options.args]\n * @param {Object} [options.scope]\n * @returns {*} [response]\n */\nfunction $exec(fn, options = {}) {\n    let fns = Object(_types__WEBPACK_IMPORTED_MODULE_0__[\"$toArray\"])(fn),\n        len = fns.length,\n        i = 0,\n        response;\n\n    for (; i < len; i++) {\n        const conf = Object(_types__WEBPACK_IMPORTED_MODULE_0__[\"_extend\"])({\n            args: [],\n        }, options);\n\n        response = fns[i].apply(\n            conf.scope,\n            Object(_types__WEBPACK_IMPORTED_MODULE_0__[\"$toArray\"])(conf.args),\n        );\n\n        if (len === 1) {\n            return response;\n        }\n    }\n}\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/core/core.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/core/dom.js":
/*!***************************************************!*\
  !*** ./node_modules/wee-core/scripts/core/dom.js ***!
  \***************************************************/
/*! exports provided: _selArray, $sel, $each, $map, $parseHTML, $ready, $setRef, $unique */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_selArray\", function() { return _selArray; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$sel\", function() { return $sel; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$each\", function() { return $each; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$map\", function() { return $map; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$parseHTML\", function() { return $parseHTML; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$ready\", function() { return $ready; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$setRef\", function() { return $setRef; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$unique\", function() { return $unique; });\n/* harmony import */ var es6_promise_auto__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! es6-promise/auto */ \"./node_modules/es6-promise/auto.js\");\n/* harmony import */ var es6_promise_auto__WEBPACK_IMPORTED_MODULE_0___default = /*#__PURE__*/__webpack_require__.n(es6_promise_auto__WEBPACK_IMPORTED_MODULE_0__);\n/* harmony import */ var _variables__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! ./variables */ \"./node_modules/wee-core/scripts/core/variables.js\");\n/* harmony import */ var _core__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! ./core */ \"./node_modules/wee-core/scripts/core/core.js\");\n/* harmony import */ var _types__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ./types */ \"./node_modules/wee-core/scripts/core/types.js\");\n\n\n\n\n\nlet range;\nlet refs = {};\n\n/**\n * Check if a node contains another node\n *\n * @private\n * @param {HTMLElement} source\n * @param {HTMLElement} target\n * @returns {boolean} match\n */\nfunction _contains(source, target) {\n    return (source === _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"] ? _variables__WEBPACK_IMPORTED_MODULE_1__[\"_html\"] : source)\n        .contains(target);\n}\n\n/**\n * Convert selection to Array\n *\n * @param {($|HTMLElement|string)} selector\n * @param {Object} [options]\n * @param {(HTMLElement|string)} [options.context=document]\n * @returns {($|Array)} nodes\n */\nfunction _selArray(selector, options) {\n    if (selector && selector._$) {\n        return selector;\n    }\n\n    options = options || {};\n\n    let el = typeof selector == 'string' ?\n        $sel(selector, options.context) :\n        selector;\n\n    return Object(_types__WEBPACK_IMPORTED_MODULE_3__[\"$toArray\"])(el);\n}\n\n/**\n * Get matches to specified selector or return parsed HTML\n *\n * @param {($|HTMLElement|string)} selector\n * @param {($|HTMLElement|string)} [context=document]\n * @returns {Array} elements\n */\nfunction $sel(selector, context) {\n    let el = null,\n        ref = [];\n\n    if (typeof selector !== 'string') {\n        el = selector;\n    } else {\n        if (selector === 'window') {\n            return [_variables__WEBPACK_IMPORTED_MODULE_1__[\"_win\"]];\n        }\n\n        if (selector === 'document') {\n            return [_variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"]];\n        }\n\n        // Return nothing if context doesn't exist\n        context = context !== _variables__WEBPACK_IMPORTED_MODULE_1__[\"U\"] ? $sel(context)[0] : _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"];\n\n        if (!context) {\n            return ref;\n        }\n\n        // Check for pre-cached elements\n        if (selector.indexOf(':') === 0 || selector.indexOf('ref:') > -1) {\n            let split = selector.split(',').filter(function (sel) {\n                sel = sel.trim();\n\n                if (sel.slice(0, 1) === ':') {\n                    sel = sel.slice(1);\n                } else if (sel.slice(0, 4) === 'ref:') {\n                    sel = sel.slice(4);\n                } else {\n                    return true;\n                }\n\n                sel = refs[sel];\n\n                // Apply context filter if not document\n                if (sel) {\n                    ref = ref.concat(\n                        context === _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"] ?\n                            sel :\n                            sel.filter(function (el) {\n                                return _contains(context, el);\n                            })\n                    );\n                }\n\n                return false;\n            });\n\n            if (split.length) {\n                selector = split.join(',');\n            } else {\n                return ref;\n            }\n        }\n\n        // Use third-party selector engine if defined\n        if (_variables__WEBPACK_IMPORTED_MODULE_1__[\"_win\"].WeeSelector !== _variables__WEBPACK_IMPORTED_MODULE_1__[\"U\"]) {\n            el = _variables__WEBPACK_IMPORTED_MODULE_1__[\"_win\"].WeeSelector(selector, context);\n        } else if (/^[#.]?[\\w-]+$/.test(selector)) {\n            let pre = selector[0];\n\n            if (pre == '#') {\n                el = _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"].getElementById(selector.substr(1));\n            } else if (pre == '.') {\n                el = context.getElementsByClassName(selector.substr(1));\n            } else {\n                el = context.getElementsByTagName(selector);\n            }\n        } else {\n            try {\n                el = context.querySelectorAll(selector);\n            } catch (e) {\n                el = $parseHTML(selector).childNodes;\n            }\n        }\n    }\n\n    if (!el) {\n        el = ref;\n    } else if (el.nodeType !== _variables__WEBPACK_IMPORTED_MODULE_1__[\"U\"] || el === _variables__WEBPACK_IMPORTED_MODULE_1__[\"_win\"]) {\n        el = [el];\n    } else {\n        el = _types__WEBPACK_IMPORTED_MODULE_3__[\"_slice\"].call(el);\n    }\n\n    // Join references if available\n    return ref.length ? el.concat(ref) : el;\n}\n\n/**\n * Execute function for each matching selection\n *\n * @param {($|Array|HTMLElement|string)} target\n * @param {Function} fn\n * @param {Object} [options]\n * @param {Array} [options.args]\n * @param {($|HTMLElement|string)} [options.context=document]\n * @param {boolean} [options.reverse=false]\n * @param {Object} [options.scope]\n */\nfunction $each(target, fn, options) {\n    if (target) {\n        let conf = Object(_types__WEBPACK_IMPORTED_MODULE_3__[\"_extend\"])({\n            args: []\n        }, options),\n            els = _selArray(target, conf),\n            i = 0;\n\n        if (conf.reverse && !els._$) {\n            els = els.reverse();\n        }\n\n        for (; i < els.length; i++) {\n            let el = els[i],\n                val = Object(_core__WEBPACK_IMPORTED_MODULE_2__[\"$exec\"])(fn, {\n                    args: [el, i].concat(conf.args),\n                    scope: conf.scope || el\n                });\n\n            if (val === false) {\n                return;\n            }\n        }\n    }\n}\n\n/**\n * Translate items in an array or selection to new Array\n *\n * @param {($|Array|HTMLElement|string)} target - array or selector\n * @param {Function} fn\n * @param {Object} [options]\n * @param {Array} [options.args]\n * @param {Object} [options.scope]\n * @returns {Array}\n */\nfunction $map(target, fn, options) {\n    if (!Array.isArray(target)) {\n        target = _selArray(target, options);\n    }\n\n    let conf = Object(_types__WEBPACK_IMPORTED_MODULE_3__[\"_extend\"])({\n        args: []\n    }, options),\n        res = [],\n        i = 0;\n\n    for (; i < target.length; i++) {\n        let el = target[i],\n            val = Object(_core__WEBPACK_IMPORTED_MODULE_2__[\"$exec\"])(fn, {\n                args: [el, i].concat(conf.args),\n                scope: conf.scope || el\n            });\n\n        if (val !== false) {\n            res.push(val);\n        }\n    }\n\n    return res;\n}\n\n/**\n * Create document fragment from an HTML string\n *\n * @param {string} html\n * @returns {HTMLElement} element\n */\nfunction $parseHTML(html) {\n    html = html.trim();\n\n    if (!range) {\n        range = _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"].createRange();\n        range.selectNode(_variables__WEBPACK_IMPORTED_MODULE_1__[\"_body\"]);\n    }\n\n    return range.createContextualFragment(html);\n}\n\n/**\n * Execute specified function when document is ready\n *\n * @param {(Array|function|string)} fn\n */\n/**\n* Execute specified function when document is ready\n*\n* @param {(Array|function|string)} fn\n*/\nfunction $ready() {\n    return new Promise((resolve) => {\n        let doc = _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"];\n\n        // This is for testing only\n        if (this && this.readyState) {\n            doc = this;\n        }\n\n        if (doc.readyState === 'complete') {\n            resolve();\n        } else {\n            doc.addEventListener('DOMContentLoaded', () => {\n                resolve();\n            });\n        }\n    });\n}\n\n/**\n * Add ref elements to datastore\n *\n * @param {(HTMLElement|string)} [context=document]\n */\nfunction $setRef(context) {\n    context = context ? $sel(context)[0] : _variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"];\n\n    // Clear existing refs if reset\n    Object.keys(refs).forEach(function (val) {\n        refs[val] = refs[val].filter(function (el) {\n            return !(\n                !_contains(_variables__WEBPACK_IMPORTED_MODULE_1__[\"_doc\"], el) ||\n                (_contains(context, el) && context !== el)\n            );\n        });\n    });\n\n    // Set refs from DOM\n    $each('[data-ref]', function (el) {\n        el.getAttribute('data-ref').split(/\\s+/)\n            .forEach(function (val) {\n                refs[val] = refs[val] || [];\n                refs[val].push(el);\n            });\n    }, {\n            context: context\n        });\n}\n\n/**\n * Create new Array with only unique values from source Array\n *\n * @param {Array} array\n * @returns {Array} unique values\n */\nfunction $unique(array) {\n    return array.reverse().filter((el, i, arr) => {\n        return arr.indexOf(el, i + 1) < 0;\n    }).reverse();\n}\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/core/dom.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/core/types.js":
/*!*****************************************************!*\
  !*** ./node_modules/wee-core/scripts/core/types.js ***!
  \*****************************************************/
/*! exports provided: _slice, _castString, _extend, $copy, $equals, $extend, $isArray, $isArrayBuffer, $isArrayBufferView, $isBlob, $isDate, $isFile, $isFormData, $isFunction, $isNumber, $isObject, $isString, $serialize, $toArray, $type, $unserialize */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_slice\", function() { return _slice; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_castString\", function() { return _castString; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_extend\", function() { return _extend; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$copy\", function() { return $copy; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$equals\", function() { return $equals; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$extend\", function() { return $extend; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isArray\", function() { return $isArray; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isArrayBuffer\", function() { return $isArrayBuffer; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isArrayBufferView\", function() { return $isArrayBufferView; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isBlob\", function() { return $isBlob; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isDate\", function() { return $isDate; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isFile\", function() { return $isFile; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isFormData\", function() { return $isFormData; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isFunction\", function() { return $isFunction; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isNumber\", function() { return $isNumber; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isObject\", function() { return $isObject; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$isString\", function() { return $isString; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$serialize\", function() { return $serialize; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$toArray\", function() { return $toArray; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$type\", function() { return $type; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$unserialize\", function() { return $unserialize; });\nconst _slice = [].slice;\n\n/**\n * Compare two arrays for equality\n *\n * @private\n * @param {Array} a\n * @param {Array} b\n * @returns {boolean}\n */\nfunction _arrEquals(a, b) {\n    return a.length == b.length &&\n        a.every((el, i) => _equals(el, b[i]));\n}\n\n/**\n * Cast string to most applicable data type\n *\n * @protected\n * @param {*} val\n */\nfunction _castString(val) {\n    if (typeof val === 'string') {\n        try {\n            val = val === 'true' ? true :\n                val === 'false' ? false :\n                    val === 'null' ? null :\n                        parseInt(val).toString() === val ? parseInt(val) :\n                            /^(?:\\{[\\w\\W]*}|\\[[\\w\\W]*])$/.test(val) ? JSON.parse(val) :\n                                val;\n        } catch (e) {}\n    }\n\n    return val;\n}\n\n/**\n * Clone value to a new instance\n *\n * @private\n * @param {*} val\n * @returns {*}\n */\nfunction _copy(val) {\n    const type = $type(val);\n\n    if (type == 'object') {\n        val = _extend({}, val, true);\n    } else if (type == 'array') {\n        val = val.slice(0);\n    }\n\n    return val;\n}\n\n/**\n * Compare two values for equality\n *\n * @private\n * @param {*} a\n * @param {*} b\n * @returns {boolean}\n */\nfunction _equals(a, b) {\n    if (a === b) {\n        return true;\n    }\n\n    const aType = $type(a);\n\n    if (aType != $type(b)) {\n        return false;\n    }\n\n    if (aType == 'array') {\n        return _arrEquals(a, b);\n    }\n\n    if (aType == 'object') {\n        return _objEquals(a, b);\n    }\n\n    if (aType == 'date') {\n        return +a == +b;\n    }\n\n    return false;\n}\n\n/**\n * Extend target object with source object(s)\n *\n * @private\n * @param {Object} target\n * @param {Object} object\n * @param {boolean} [deep=false]\n * @param {Array} [_set=[]]\n * @returns {Object}\n */\nfunction _extend(target, object, deep, _set = []) {\n    if (! object) {\n        return target;\n    }\n\n    for (const key in object) {\n        let src = object[key],\n            type = $type(src);\n\n        if (deep && type == 'object') {\n            let len = _set.length,\n                i = 0,\n                val;\n\n            for (; i < len; i++) {\n                if (_set[i] === src) {\n                    val = src;\n                    break;\n                }\n            }\n\n            if (val) {\n                target[key] = val;\n            } else {\n                _set.push(src);\n                target[key] = _extend(target[key] || {}, src, deep, _set);\n            }\n        } else if (src !== undefined) {\n            target[key] = type == 'array' ? src.slice(0) : src;\n        }\n    }\n\n    return target;\n}\n\n/**\n * Compare two objects for equality\n *\n * @private\n * @param {Object} a\n * @param {Object} b\n * @returns {boolean}\n */\nfunction _objEquals(a, b) {\n    const aKeys = Object.keys(a);\n\n    return _arrEquals(aKeys.sort(), Object.keys(b).sort()) &&\n        aKeys.every(i => _equals(a[i], b[i]));\n}\n\n/**\n * Clone value to a new instance\n *\n * @private\n * @param {*} val\n * @returns {*}\n */\nfunction $copy(val) {\n    return _copy(val);\n}\n\n/**\n * Compare two values for strict equality\n *\n * @param {*} a\n * @param {*} b\n * @returns {boolean}\n */\nfunction $equals(a, b) {\n    return _equals(a, b);\n}\n\n/**\n * Extend target object with source object(s)\n *\n * @param {(boolean|Object)} deep - extend nested properties else target object\n * @param {Object} [obj] - target object\n * @param {...Object} [obj] - merged objects\n * @returns {Object}\n */\nfunction $extend(deep) {\n    let bool = typeof deep === 'boolean',\n        args = _slice.call(arguments).slice(bool ? 1 : 0),\n        target = args[0] || {};\n    deep = bool ? deep : false;\n\n    args.slice(1).forEach((source) => {\n        target = _extend(target, source, deep);\n    });\n\n    return target;\n}\n\n/**\n * Determine if value is an array\n *\n * @param {*} obj\n * @returns {boolean}\n */\nfunction $isArray(obj) {\n    return Array.isArray(obj);\n}\n\n/**\n * Determine if a value is an ArrayBuffer\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an ArrayBuffer, otherwise false\n */\nfunction $isArrayBuffer(val) {\n    return $type(val) === 'arraybuffer';\n}\n\n/**\n * Determine if a value is a view on an ArrayBuffer\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a view on an ArrayBuffer, otherwise false\n */\nfunction $isArrayBufferView(val) {\n    let result;\n\n    // IE10 support and up\n    if (typeof ArrayBuffer !== 'undefined' && ArrayBuffer.isView) {\n        result = ArrayBuffer.isView(val);\n    }\n\n    return result;\n}\n\n/**\n * Determine if a value is a Blob\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Blob, otherwise false\n */\nfunction $isBlob(val) {\n    return $type(val) === 'blob';\n}\n\n/**\n * Determine if a value is a Date\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a Date, otherwise false\n */\nfunction $isDate(val) {\n    return $type(val) === 'date';\n}\n\n/**\n * Determine if a value is a File\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is a File, otherwise false\n */\nfunction $isFile(val) {\n    return $type(val) === 'file';\n}\n\n/**\n * Determine if a value is a FormData\n *\n * @param {Object} val The value to test\n * @returns {boolean} True if value is an FormData, otherwise false\n */\nfunction $isFormData(val) {\n    return (typeof FormData !== 'undefined') && (val instanceof FormData);\n}\n\n/**\n * Determine if value is a function\n *\n * @param {*} obj\n * @returns {boolean}\n */\nfunction $isFunction(obj) {\n    return $type(obj) === 'function';\n}\n\n/**\n * Determine if value is a number (optional loose match)\n *\n * @param {*} obj\n * @param {boolean} [strict]\n * @returns {boolean}\n */\nfunction $isNumber(obj, strict = true) {\n    if (! strict) {\n        if (! obj.match(/^\\d*\\.?\\d*$/g)) {\n            return false;\n        }\n\n        const value = parseFloat(obj);\n\n        // If value = NaN, will not be equal\n        return value === value;\n    }\n\n    return $type(obj) === 'number';\n}\n\n/**\n * Determine if value is an object\n *\n * @param {*} obj\n * @returns {boolean}\n */\nfunction $isObject(obj) {\n    return $type(obj) === 'object';\n}\n\n/**\n * Determine if value is a string\n *\n * @param {*} obj\n * @returns {boolean}\n */\nfunction $isString(obj) {\n    return typeof obj === 'string';\n}\n\n/**\n * Serialize object\n *\n * @param {Object} obj\n * @returns {string} value\n */\nfunction $serialize(obj) {\n    const arr = [];\n\n    Object.keys(obj || {}).forEach((key) => {\n        const val = obj[key];\n        key = encodeURIComponent(key);\n\n        if (typeof val !== 'object') {\n            arr.push(`${key}=${encodeURIComponent(val)}`);\n        } else if (Array.isArray(val)) {\n            val.forEach((el) => {\n                arr.push(`${key}[]=${encodeURIComponent(el)}`);\n            });\n        }\n    });\n\n    return arr.length ? arr.join('&').replace(/%20/g, '+') : '';\n}\n\n/**\n * Cast value to array if it isn't one\n *\n * @param {*} val\n * @returns {Array} value\n */\nfunction $toArray(val) {\n    return val !== undefined ? (Array.isArray(val) ? val : [val]) : [];\n}\n\n/**\n * Determine the JavaScript type of an object\n *\n * @param {*} obj\n * @returns string\n */\nfunction $type(obj) {\n    return obj === undefined ? 'undefined' :\n        Object.prototype.toString.call(obj)\n            .replace(/^\\[object (.+)]$/, '$1')\n            .toLowerCase();\n}\n\n/**\n * Convert serialized string back into an object\n *\n * @param {string} str\n * @returns {Object} value\n */\nfunction $unserialize(str) {\n    const obj = {};\n\n    decodeURIComponent(str)\n        .replace(/^\\?/, '')\n        .split('&').forEach((el) => {\n            let split = el.split('='),\n                key = split[0].replace('[]', ''),\n                val = (split[1] || '').replace(/\\+/g, ' ') || '',\n                isArrayProp = /\\[\\]/.test(split[0]);\n\n            if (obj.hasOwnProperty(key)) {\n                obj[key] = $toArray(obj[key]);\n                obj[key].push(_castString(val));\n            } else {\n                obj[key] = isArrayProp ? [_castString(val)] : _castString(val);\n            }\n        });\n\n    return obj;\n}\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/core/types.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/core/variables.js":
/*!*********************************************************!*\
  !*** ./node_modules/wee-core/scripts/core/variables.js ***!
  \*********************************************************/
/*! exports provided: _$, _doc, _body, _html, _win, U */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* WEBPACK VAR INJECTION */(function(global) {/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_$\", function() { return _$; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_doc\", function() { return _doc; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_body\", function() { return _body; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_html\", function() { return _html; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"_win\", function() { return _win; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"U\", function() { return U; });\n/* harmony import */ var _core__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./core */ \"./node_modules/wee-core/scripts/core/core.js\");\n\n\nconst D = _core__WEBPACK_IMPORTED_MODULE_0__[\"isBrowser\"] ? window.document : {};\nconst scope = _core__WEBPACK_IMPORTED_MODULE_0__[\"isBrowser\"] ? window : global;\n\n// Export globals for use in other modules\nconst _$ = scope.WeeAlias || '$';\nconst _doc = D;\nconst _body = D.body;\nconst _html = D.documentElement;\nconst _win = scope;\nconst U = undefined;\n\n/* WEBPACK VAR INJECTION */}.call(this, __webpack_require__(/*! ./../../../webpack/buildin/global.js */ \"./node_modules/webpack/buildin/global.js\")))\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/core/variables.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/core/warn.js":
/*!****************************************************!*\
  !*** ./node_modules/wee-core/scripts/core/warn.js ***!
  \****************************************************/
/*! exports provided: warn */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"warn\", function() { return warn; });\n/**\n * Print warning to console if under development and condition is not met\n *\n * @param {*} condition\n * @param {string} module\n * @param {string} message\n */\nfunction warn(module, message) {\n    if (true) {\n        typeof console.warn(`[wee-${module}] ${message}`);\n    }\n}\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/core/warn.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/store/error.js":
/*!******************************************************!*\
  !*** ./node_modules/wee-core/scripts/store/error.js ***!
  \******************************************************/
/*! exports provided: default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"default\", function() { return StoreError; });\nclass StoreError extends Error {\n    constructor(message) {\n        super(message);\n\n        this.errorType = 'StoreError';\n    }\n}\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/store/error.js?");

/***/ }),

/***/ "./node_modules/wee-core/scripts/wee-store.js":
/*!****************************************************!*\
  !*** ./node_modules/wee-core/scripts/wee-store.js ***!
  \****************************************************/
/*! exports provided: Store, $setVar, destroyStore, default */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"Store\", function() { return Store; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"$setVar\", function() { return $setVar; });\n/* harmony export (binding) */ __webpack_require__.d(__webpack_exports__, \"destroyStore\", function() { return destroyStore; });\n/* harmony import */ var core_core__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! core/core */ \"./node_modules/wee-core/scripts/core/core.js\");\n/* harmony import */ var core_types__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core/types */ \"./node_modules/wee-core/scripts/core/types.js\");\n/* harmony import */ var core_dom__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! core/dom */ \"./node_modules/wee-core/scripts/core/dom.js\");\n/* harmony import */ var core_variables__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! core/variables */ \"./node_modules/wee-core/scripts/core/variables.js\");\n/* harmony import */ var core_warn__WEBPACK_IMPORTED_MODULE_4__ = __webpack_require__(/*! core/warn */ \"./node_modules/wee-core/scripts/core/warn.js\");\n/* harmony import */ var store_error__WEBPACK_IMPORTED_MODULE_5__ = __webpack_require__(/*! store/error */ \"./node_modules/wee-core/scripts/store/error.js\");\n\n\n\n\n\n\n\nlet instances = {};\n\n/**\n * Create wrapper for browser storage api\n *\n * @param {string} type\n * @returns {*}\n * @private\n */\nfunction _storageFactory(type) {\n\tlet storage;\n\n\tif (type === 'local') {\n\t\tstorage = window.localStorage;\n\t} else if (type === 'session') {\n\t\tstorage = window.sessionStorage;\n\t}\n\n\treturn {\n\t\tgetItem(key) {\n\t\t\treturn JSON.parse(storage.getItem(key));\n\t\t},\n\t\tsetItem(key, value) {\n\t\t\tvalue = JSON.stringify(value);\n\n\t\t\treturn storage.setItem(key, value);\n\t\t},\n\t\tremoveItem(key) {\n\t\t\treturn storage.removeItem(key);\n\t\t}\n\t};\n}\n\nclass Store {\n\tconstructor(name, options = {}) {\n\t\tthis.localStorage = _storageFactory('local');\n\t\tthis.sessionStorage = _storageFactory('session');\n\n\t\tthis._setBrowserStorage(options.browserStorage, false);\n\n\t\tthis.name = name;\n\t\tthis.keepInMemory = options.keepInMemory || true;\n\t\tthis.prefix = options.prefix || 'wee';\n\t\tthis.browserStoreKey = `${this.prefix}_${this.name}`;\n\n\t\tif (this.browserStore && this.browserStore.getItem(this.browserStoreKey)) {\n\t\t\tthis.store = this.browserStore.getItem(this.browserStoreKey)\n\t\t} else {\n\t\t\tthis._syncStore({ $: {} });\n\t\t}\n\n\t\tthis.observe = {\n\t\t\t$: {}\n\t\t};\n\t}\n\n\t/**\n\t * Push or concatenate values into array\n\t *\n\t * @param {string} type - 'concat' or 'push'\n\t * @param {Object} store\n\t * @param {Object} obs\n\t * @param {string} key\n\t * @param {Array|*} val\n\t * @param {boolean} prepend\n\t * @param {boolean} [sync = true]\n\t * @returns {*}\n\t * @private\n\t */\n\t_add(type, store, obs, key, val, prepend, sync = true) {\n\t\tif (prepend === core_variables__WEBPACK_IMPORTED_MODULE_3__[\"U\"]) {\n\t\t\tprepend = val;\n\t\t\tval = key;\n\t\t}\n\n\t\tconst stored = this._storage(store, key, true);\n\t\tconst seg = stored[1];\n\t\tconst orig = Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$copy\"])(stored[2]);\n\t\tlet root = stored[0];\n\n\t\t// If store property is not already an array, set to empty array\n\t\tif (! Array.isArray(orig)) {\n\t\t\troot[seg] = [];\n\t\t}\n\n\t\tif (type === 'concat') {\n\t\t\troot[seg] = prepend ?\n\t\t\t\tObject(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$toArray\"])(val).concat(root[seg]) :\n\t\t\t\troot[seg].concat(val);\n\t\t} else {\n\t\t\tprepend ?\n\t\t\t\troot[seg].unshift(val) :\n\t\t\t\troot[seg].push(val);\n\t\t}\n\n\t\tif (sync) {\n\t\t\tthis._syncStore(store);\n\t\t}\n\n\t\treturn root[seg];\n\t}\n\n\t/**\n\t * Get variable\n\t *\n\t * @param {Object} store\n\t * @param {Object} obs\n\t * @param {string} key\n\t * @param {*} fallback\n\t * @param {boolean} [set=false]\n\t * @param {Object} [options={}]\n\t * @returns {*}\n\t * @private\n\t */\n\t_get(store, obs, key, fallback, set = false, options = {}) {\n\t\tconst resp = this._storage(store, key)[2];\n\n\t\tif (resp !== core_variables__WEBPACK_IMPORTED_MODULE_3__[\"U\"]) {\n\t\t\treturn resp;\n\t\t}\n\n\t\tif (fallback !== core_variables__WEBPACK_IMPORTED_MODULE_3__[\"U\"]) {\n\t\t\treturn set ?\n\t\t\t\tthis._set(store, obs, key, fallback, options) :\n\t\t\t\tthis._val(fallback, options);\n\t\t}\n\n\t\treturn null;\n\t}\n\n\t/**\n\t * Ensure that in memory store is up to date\n\t *\n\t * @param {Object} store\n\t * @private\n\t */\n\t_syncStore(store) {\n\t\tif (this.browserStore) {\n\t\t\tthis.browserStore.setItem(this.browserStoreKey, store);\n\t\t}\n\n\t\tif (this.keepInMemory) {\n\t\t\tthis.store = store;\n\t\t}\n\t}\n\n\t/**\n\t * Determine data storage root, key, and value\n\t *\n\t * @param {Object} obj\n\t * @param {string} key\n\t * @param {boolean} [create=false]\n\t * @returns {Array} value\n\t */\n\t_storage(obj, key, create = false) {\n\t\tlet data = obj;\n\t\tlet\ttype = Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$type\"])(key);\n\t\tlet num = type == 'number';\n\t\tlet val;\n\n\t\tif (num || type == 'string') {\n\t\t\tlet segs = key.toString().split('.');\n\t\t\tkey = segs.pop();\n\t\t\tdata = data.$;\n\n\t\t\tsegs.forEach(function(key) {\n\t\t\t\tdata = data.hasOwnProperty(key) ?\n\t\t\t\t\tdata[key] :\n\t\t\t\t\t(create ? data[key] = {} : []);\n\t\t\t});\n\t\t} else {\n\t\t\tkey = '$';\n\t\t}\n\n\t\tif (num && Array.isArray(data)) {\n\t\t\tvar arr = data.slice(key);\n\n\t\t\tif (arr.length) {\n\t\t\t\tval = arr[0];\n\t\t\t}\n\t\t} else {\n\t\t\tkey = key.toString();\n\n\t\t\tif (data.hasOwnProperty(key)) {\n\t\t\t\tval = data[key];\n\t\t\t}\n\t\t}\n\n\t\treturn [data, key, val];\n\t}\n\n\t/**\n\t * Set variable\n\t *\n\t * @param {Object} store\n\t * @param {Object} obs\n\t * @param {string} key\n\t * @param {*} val\n\t * @param {Object} [options={}]\n\t * @param {boolean} [sync=true]\n\t * @returns {*}\n\t * @private\n\t */\n\t_set(store, obs, key, val, options = {}, sync = true) {\n\t\tlet stored = this._storage(store, key, true);\n\t\tlet seg = stored[1];\n\t\tlet data = seg === '$' ?\n\t\t\t\tthis._val(key, val) :\n\t\t\t\tthis._val(val, options);\n\n\t\tstored[0][seg] = data;\n\n\t\tif (sync) {\n\t\t\tthis._syncStore(store);\n\t\t}\n\n\t\treturn data;\n\t}\n\n\t/**\n\t * Set a browser storage to be used for persistence of data\n\t *\n\t * @param {string} storageType\n\t * @param {boolean} [sync=true]\n\t */\n\t_setBrowserStorage(storageType, sync = true) {\n\t\tif (typeof storageType === 'string') {\n\t\t\tthis.browserStore = storageType === 'local' ?\n\t\t\t\tthis.localStorage :\n\t\t\t\tthis.sessionStorage;\n\n\t\t\t// If store already contains data that needs to be saved to browser storage\n\t\t\tif (sync) {\n\t\t\t\tthis.browserStore.setItem(this.browserStoreKey, this.store);\n\t\t\t}\n\t\t} else {\n\t\t\tthis.browserStore = null;\n\t\t}\n\t}\n\n\t/**\n\t * Get value from function or directly\n\t *\n\t * @private\n\t * @param {*} val\n\t * @param {Object} [options]\n\t * @returns {*}\n\t */\n\t_val(val, options) {\n\t\treturn Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$isFunction\"])(val) ?\n\t\t\tObject(core_core__WEBPACK_IMPORTED_MODULE_0__[\"$exec\"])(val, options) :\n\t\t\tval;\n\t}\n\n\t/**\n\t * Configure instance after initial creation\n\t *\n\t * @param {Object} options\n\t */\n\tconfigure(options) {\n\t\tif (options.hasOwnProperty('browserStorage')) {\n\t\t\tthis._setBrowserStorage(options.browserStorage);\n\t\t}\n\n\t\tif (options.hasOwnProperty('keepInMemory')) {\n\t\t\tthis.keepInMemory = options.keepInMemory;\n\t\t}\n\t}\n\n\t/**\n\t * Get variable\n\t *\n\t * @param {string} key\n\t * @param {*} [fallback]\n\t * @param {boolean} [set=false]\n\t * @param {Object} [options] - available for fallback functions\n\t * @param {Array} [options.args]\n\t * @param {Object} [options.scope]\n\t * @returns {*} value\n\t */\n\tget(key, fallback, set = false, options) {\n\t\treturn this._get(this.getStore(), this.observe, key, fallback, set, options);\n\t}\n\n\t/**\n\t * Return storage object from either memory or localStorage/sessionStorage\n\t *\n\t * @returns {Object}\n\t * @private\n\t */\n\tgetStore() {\n\t\tif (! this.keepInMemory && this.browserStore) {\n\t\t\treturn this.browserStore.getItem(this.browserStoreKey);\n\t\t}\n\n\t\treturn this.store;\n\t}\n\n\t/**\n\t * Set variable\n\t *\n\t * @param {string} key\n\t * @param {*} val\n\t * @param {Object} [options] - applicable if value is a callback\n\t * @param {Array} [options.args]\n\t * @param {Object} [options.scope]\n\t * @returns {*} value\n\t */\n\tset(key, val, options) {\n\t\treturn this._set(this.getStore(), this.observe, key, val, options);\n\t}\n\n\t/**\n\t * Check if storage criteria is set\n\t *\n\t * @param {string} key\n\t * @param {*} [val]\n\t * @returns {boolean}\n\t */\n\thas(key, val) {\n\t\tconst resp = this._storage(this.getStore(), key)[2];\n\n\t\tif (resp === core_variables__WEBPACK_IMPORTED_MODULE_3__[\"U\"]) {\n\t\t\treturn false;\n\t\t}\n\n\t\tif (val !== core_variables__WEBPACK_IMPORTED_MODULE_3__[\"U\"]) {\n\t\t\tif (Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$isObject\"])(resp)) {\n\t\t\t\treturn resp.hasOwnProperty(val);\n\t\t\t}\n\n\t\t\tif (Array.isArray(resp)) {\n\t\t\t\treturn resp.indexOf(val) > -1;\n\t\t\t}\n\n\t\t\treturn resp === val;\n\t\t}\n\n\t\treturn true;\n\t}\n\n\t/**\n\t * Push value into global array\n\t *\n\t * @param {string} key\n\t * @param {*} val\n\t * @param {boolean} [prepend=false]\n\t * @returns {Array|Object} value\n\t */\n\tpush(key, val, prepend = false) {\n\t\treturn this._add('push', this.getStore(), this.observe, key, val, prepend);\n\t}\n\n\t/**\n\t * Concatenate values into global storage\n\t *\n\t * @param {string} key\n\t * @param {*} val\n\t * @param {boolean} [prepend=false]\n\t * @returns {Array|Object} value\n\t */\n\tconcat(key, val, prepend = false) {\n\t\treturn this._add('concat', this.getStore(), this.observe, key, val, prepend);\n\t}\n\n\t/**\n\t * Extend object into global storage\n\t *\n\t * @param {string} key\n\t * @param {Object} val\n\t * @returns {Object} value\n\t */\n\tmerge(key, val) {\n\t\tconst store = this.getStore();\n\t\tval = Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$extend\"])(true, {}, this._get(store, this.observe, key, {}), val);\n\n\t\treturn this._set(store, this.observe, key, val);\n\t}\n\n\t/**\n\t * Remove top-level store property\n\t *\n\t * @param {string} key\n\t * @returns {*}\n\t */\n\tdrop(key) {\n\t\tconst store = this.getStore();\n\t\tconst stored = this._storage(store, key);\n\t\tlet root = stored[0];\n\t\tlet seg = stored[1];\n\t\tlet orig = Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"$copy\"])(stored[2]);\n\t\tlet arr = Array.isArray(root);\n\n\t\tarr ?\n\t\t\troot.splice(seg, 1) :\n\t\t\tdelete root[seg];\n\n\t\tthis._syncStore(store);\n\n\t\treturn orig;\n\t}\n\n\t/**\n\t * Set variables from DOM\n\t *\n\t * @param {string} store\n\t * @param {($|HTMLElement|string)} context\n\t */\n\tsetVar(context) {\n\t\tconst store = this.getStore();\n\t\tconst storeFilter = this.name !== 'default' ?\n\t\t\t`[data-store=\"${this.name}\"]` :\n\t\t\t':not([data-store])';\n\n\t\tObject(core_dom__WEBPACK_IMPORTED_MODULE_2__[\"$each\"])(`[data-set]${storeFilter}`, (el) => {\n\t\t\t_setVar(this, el);\n\t\t}, {\n\t\t\tcontext: context\n\t\t});\n\n\t\t// Prevented _syncStore in _add and _set inside $each for performance\n\t\tthis._syncStore(store);\n\t}\n\n\t/**\n\t * Remove reference to store instance\n\t */\n\tdestroy() {\n\t\tdelete instances[this.name];\n\n\t\tif (this.browserStore) {\n\t\t\tthis.browserStore.removeItem(this.browserStoreKey);\n\t\t}\n\t}\n}\n\nconst store = new Store('default');\n\nfunction _setVar(instance, el) {\n\tconst key = el.getAttribute('data-set');\n\tconst val = Object(core_types__WEBPACK_IMPORTED_MODULE_1__[\"_castString\"])(el.getAttribute('data-value'));\n\tconst store = instance.getStore();\n\n\tkey.slice(-2) == '[]' ?\n\t\tinstance._add('push', store, instance.observe, key.slice(0, -2), val, false, false) :\n\t\tinstance._set(store, instance.observe, key, val, {}, false);\n}\n\n/**\n * Refresh all stores\n *\n * @param context\n */\nfunction $setVar(context) {\n\tObject(core_dom__WEBPACK_IMPORTED_MODULE_2__[\"$each\"])('[data-set]', (el) => {\n\t\tconst name = el.getAttribute('data-store');\n\t\tconst instance = name && instances[name] ? instances[name] : store;\n\n\t\t_setVar(instance, el);\n\t}, {\n\t\tcontext,\n\t});\n}\n\n/**\n * Instantiate new Store instance\n *\n * @param {string} name\n * @param {Object} options\n * @returns {*}\n */\nstore.create = function createStore(name, options = {}) {\n\tif (! name) {\n\t\tthrow new store_error__WEBPACK_IMPORTED_MODULE_5__[\"default\"]('No name provided when creating new store instance');\n\t}\n\n\tif (instances[name]) {\n\t\tObject(core_warn__WEBPACK_IMPORTED_MODULE_4__[\"warn\"])('store', `creation of a store instance named ${name} was attempted but already exists`);\n\t\treturn instances[name];\n\t}\n\n\tconst instance = new Store(name, options);\n\n\tinstances[instance.name] = instance;\n\n\treturn instance;\n}\n\n/**\n * Return all store instances\n *\n * @param {string} name\n * @returns {*}\n */\nstore.instances = function getInstances(name) {\n\tif (name) {\n\t\treturn instances[name];\n\t}\n\n\treturn instances;\n}\n\n/**\n * Destroy store instance\n *\n * @param {string} name\n */\nfunction destroyStore(name) {\n\tif (instances[name]) {\n\t\tinstances[name].destroy();\n\t}\n}\n\n/* harmony default export */ __webpack_exports__[\"default\"] = (store);\n\n\n//# sourceURL=webpack:///./node_modules/wee-core/scripts/wee-store.js?");

/***/ }),

/***/ "./source/components sync recursive \\.scss$":
/*!****************************************!*\
  !*** ./source/components sync \.scss$ ***!
  \****************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("var map = {\n\t\"./welcome/index.scss\": \"./source/components/welcome/index.scss\"\n};\n\n\nfunction webpackContext(req) {\n\tvar id = webpackContextResolve(req);\n\treturn __webpack_require__(id);\n}\nfunction webpackContextResolve(req) {\n\tvar id = map[req];\n\tif(!(id + 1)) { // check for number or string\n\t\tvar e = new Error(\"Cannot find module '\" + req + \"'\");\n\t\te.code = 'MODULE_NOT_FOUND';\n\t\tthrow e;\n\t}\n\treturn id;\n}\nwebpackContext.keys = function webpackContextKeys() {\n\treturn Object.keys(map);\n};\nwebpackContext.resolve = webpackContextResolve;\nmodule.exports = webpackContext;\nwebpackContext.id = \"./source/components sync recursive \\\\.scss$\";\n\n//# sourceURL=webpack:///./source/components_sync_\\.scss$?");

/***/ }),

/***/ "./source/components/welcome/index.scss":
/*!**********************************************!*\
  !*** ./source/components/welcome/index.scss ***!
  \**********************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("// extracted by mini-css-extract-plugin\n\n//# sourceURL=webpack:///./source/components/welcome/index.scss?");

/***/ }),

/***/ "./source/scripts/app.js":
/*!*******************************!*\
  !*** ./source/scripts/app.js ***!
  \*******************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var _bootstrap__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! ./bootstrap */ \"./source/scripts/bootstrap.js\");\n\n\n// Begin your application here\n// Example:\n\n// import $router from 'wee-routes';\n\n// $router.map([\n//     { path: '/', init() { 'Hello World' } }\n// ]).run();\n\n\n//# sourceURL=webpack:///./source/scripts/app.js?");

/***/ }),

/***/ "./source/scripts/bootstrap.js":
/*!*************************************!*\
  !*** ./source/scripts/bootstrap.js ***!
  \*************************************/
/*! no exports provided */
/***/ (function(module, __webpack_exports__, __webpack_require__) {

"use strict";
eval("__webpack_require__.r(__webpack_exports__);\n/* harmony import */ var wee_store__WEBPACK_IMPORTED_MODULE_0__ = __webpack_require__(/*! wee-store */ \"./node_modules/wee-core/scripts/wee-store.js\");\n/* harmony import */ var core_dom__WEBPACK_IMPORTED_MODULE_1__ = __webpack_require__(/*! core/dom */ \"./node_modules/wee-core/scripts/core/dom.js\");\n/* harmony import */ var es6_promise_auto__WEBPACK_IMPORTED_MODULE_2__ = __webpack_require__(/*! es6-promise/auto */ \"./node_modules/es6-promise/auto.js\");\n/* harmony import */ var es6_promise_auto__WEBPACK_IMPORTED_MODULE_2___default = /*#__PURE__*/__webpack_require__.n(es6_promise_auto__WEBPACK_IMPORTED_MODULE_2__);\n/* harmony import */ var _styles_global_scss__WEBPACK_IMPORTED_MODULE_3__ = __webpack_require__(/*! ../styles/global.scss */ \"./source/styles/global.scss\");\n/* harmony import */ var _styles_global_scss__WEBPACK_IMPORTED_MODULE_3___default = /*#__PURE__*/__webpack_require__.n(_styles_global_scss__WEBPACK_IMPORTED_MODULE_3__);\n\n\n\n\n\n// Import all component scss files\n__webpack_require__(\"./source/components sync recursive \\\\.scss$\");\n\nObject(core_dom__WEBPACK_IMPORTED_MODULE_1__[\"$setRef\"])();\nObject(wee_store__WEBPACK_IMPORTED_MODULE_0__[\"$setVar\"])();\n\n\n//# sourceURL=webpack:///./source/scripts/bootstrap.js?");

/***/ }),

/***/ "./source/styles/global.scss":
/*!***********************************!*\
  !*** ./source/styles/global.scss ***!
  \***********************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("// extracted by mini-css-extract-plugin\n\n//# sourceURL=webpack:///./source/styles/global.scss?");

/***/ }),

/***/ 0:
/*!*************************************!*\
  !*** multi ./source/scripts/app.js ***!
  \*************************************/
/*! no static exports found */
/***/ (function(module, exports, __webpack_require__) {

eval("module.exports = __webpack_require__(/*! /Users/peterbedorjr/projects/work/new-wee/source/scripts/app.js */\"./source/scripts/app.js\");\n\n\n//# sourceURL=webpack:///multi_./source/scripts/app.js?");

/***/ })

/******/ });